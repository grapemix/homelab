---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_ACME_EMAIL: "{{ bootstrap_acme_email }}"
  EMAIL_DOMAIN: "{{ bootstrap_cloudflare_domain }}"
